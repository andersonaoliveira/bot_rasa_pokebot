dev:
  Bot-1:
    image: rasa/rasa
    command: bash
    sync:
      - .:/usr/src/app
    forward:
      - 8080:8080
    reverse:
      - 9000:9000
    autocreate: true
    volumes:
      - /root/.cache/pip
